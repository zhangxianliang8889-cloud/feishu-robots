#!/usr/bin/env python3
# -*- coding: utf-8 -*-
"""
预防机制总结文档
"""

print("""
================================================================================
📋 预防机制总结 - 避免未知用户问题再次发生
================================================================================

## 🔍 问题根源

1. **API权限缺失**：机器人缺少获取用户信息的权限
2. **已退群用户**：用户退群后不在成员列表中
3. **已离职用户**：用户离职后API也获取不到姓名

## ✅ 已实施的解决方案

### 1. 多层用户名获取机制

```
获取用户名流程：
┌─────────────────────────────────────────────────────────────┐
│ 1. 当前群成员列表（最新数据）                                │
│    ↓ 如果没有                                                │
│ 2. 用户名缓存（历史数据）                                    │
│    ↓ 如果没有                                                │
│ 3. 飞书API查询（实时获取）                                   │
│    ↓ 如果没有                                                │
│ 4. 手动维护映射（人工补充）                                  │
│    ↓ 如果没有                                                │
│ 5. 显示"已退群用户(ID后8位)"                                 │
└─────────────────────────────────────────────────────────────┘
```

### 2. 用户名缓存系统

文件：`user_name_cache.py`

功能：
- 从所有群的成员列表中收集用户信息
- 从API获取用户信息并缓存
- 支持手动维护已退群用户映射

缓存文件：
- `user_name_cache.json` - 自动缓存
- `left_users_mapping.json` - 手动维护

### 3. 未知用户检测器

文件：`unknown_user_detector.py`

功能：
- 自动检测不在成员列表中的用户
- 记录未知用户的出现时间和消息数
- 生成未知用户报告

日志文件：`unknown_users_log.json`

## 🛡️ 预防措施

### 1. 定期检查

建议每周运行一次检查脚本：

```bash
cd /Users/yueguangbaohe/Documents/trae_projects/pi_digits
python3 init_user_cache.py  # 更新缓存
```

### 2. 手动维护

当发现新的未知用户时：

1. 查看 `unknown_users_log.json` 了解详情
2. 在 `left_users_mapping.json` 中填写姓名：

```json
{
  "ou_xxxxxxxx": {
    "name": "真实姓名",
    "status": "left",
    "note": "备注信息"
  }
}
```

### 3. API权限维护

确保机器人权限：
- `contact:contact.base:readonly` - 获取用户基本信息

权限申请地址：
- 群消息统计机器人：https://open.feishu.cn/app/cli_a92aab4685f9dbc7/auth
- 群会议纪要机器人：https://open.feishu.cn/app/cli_a9233dfe18389bde/auth

## 📊 监控指标

### 1. 未知用户比例

- 目标：< 5%
- 警告：> 10%
- 紧急：> 20%

### 2. 缓存覆盖率

- 目标：> 95%
- 警告：< 90%
- 紧急：< 80%

## 🔧 故障排除

### 问题1：仍然显示"已退群用户"

解决方案：
1. 检查API权限是否已开通
2. 运行 `init_user_cache.py` 更新缓存
3. 手动在 `left_users_mapping.json` 中填写姓名

### 问题2：API返回空姓名

原因：用户已离职或被删除

解决方案：
- 只能通过 `left_users_mapping.json` 手动维护

### 问题3：缓存文件损坏

解决方案：
```bash
# 重新初始化缓存
cd /Users/yueguangbaohe/Documents/trae_projects/pi_digits
python3 init_user_cache.py
```

## 📝 维护清单

- [ ] 每周运行 `init_user_cache.py` 更新缓存
- [ ] 每月检查 `unknown_users_log.json`
- [ ] 及时更新 `left_users_mapping.json`
- [ ] 定期检查API权限是否有效

================================================================================
""")
