# 阿里云服务器手动部署指南

## 📋 服务器信息

- **IP地址**: 47.100.10.100
- **用户名**: xuankong
- **密码**: xkxx@2021
- **部署目录**: /root/feishu_bots

---

## 🚀 部署步骤

### 1️⃣ 连接服务器

```bash
ssh xuankong@47.100.10.100
# 输入密码: xkxx@2021
```

### 2️⃣ 创建部署目录

```bash
mkdir -p /root/feishu_bots
cd /root/feishu_bots
```

### 3️⃣ 上传代码

**在本地Mac电脑上执行：**

```bash
cd /Users/yueguangbaohe/Documents/trae_projects/pi_digits
scp -r . xuankong@47.100.10.100:/root/feishu_bots/
```

### 4️⃣ 安装依赖

**在服务器上执行：**

```bash
cd /root/feishu_bots
pip3 install requests schedule paramiko
```

### 5️⃣ 安装screen（用于后台运行）

```bash
# CentOS/RHEL
yum install screen -y

# Ubuntu/Debian
apt install screen -y
```

### 6️⃣ 启动机器人

```bash
cd /root/feishu_bots/群会议纪要机器人

# 方式1：直接运行（测试用）
python3 start_daemon.py

# 方式2：使用screen后台运行（推荐）
screen -dmS bots bash -c "python3 start_daemon.py"
```

### 7️⃣ 查看运行状态

```bash
# 查看Python进程
ps aux | grep python

# 查看screen会话
screen -ls

# 进入screen会话
screen -r bots

# 退出screen会话（保持程序运行）
按 Ctrl+A+D
```

### 8️⃣ 查看日志

```bash
# 实时查看日志
tail -f /root/feishu_bots/群会议纪要机器人/logs/*.log

# 查看最新日志
tail -n 100 /root/feishu_bots/群会议纪要机器人/logs/*.log
```

---

## 📊 定时任务时间

### 群会议纪要机器人
- **日报**: 每天 21:00
- **周报**: 每周一 09:00
- **月报**: 每月最后一天 09:30

### 群消息统计机器人
- **日报**: 每天 09:00
- **周报**: 每周一 09:00
- **月报**: 每月1号 09:00

---

## 🔧 常用命令

### 启动机器人
```bash
cd /root/feishu_bots/群会议纪要机器人
screen -dmS bots bash -c "python3 start_daemon.py"
```

### 停止机器人
```bash
# 进入screen会话
screen -r bots

# 按 Ctrl+C 停止程序
```

### 重启机器人
```bash
# 停止
screen -r bots
# 按 Ctrl+C

# 启动
cd /root/feishu_bots/群会议纪要机器人
screen -dmS bots bash -c "python3 start_daemon.py"
```

### 查看日志
```bash
tail -f /root/feishu_bots/群会议纪要机器人/logs/*.log
```

---

## ⚠️ 注意事项

1. **保持服务器运行**: 机器人需要24小时运行
2. **网络连接**: 确保服务器能访问飞书API
3. **定期检查**: 建议定期查看日志，确保正常运行
4. **自动重启**: 守护进程会自动重启崩溃的程序
5. **断网重连**: 网络恢复后会自动重连

---

## 🎉 部署完成

机器人已部署到服务器，会自动定时发送报告！

如有问题，请查看日志文件排查。
